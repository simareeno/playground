{% if brand_setting('user.downloads_subscriptions') %}
    <a href="{{ path('portal_downloads_root_category_toggle_subscription') }}" class="post-link">
      {% if is_subscribed %}
        {{ phrase('portal.general.btn-unsubscribe') }}
      {% else %}
          {{ phrase('portal.general.btn-subscribe') }}
      {% endif %}
    </a>
{% endif %}
