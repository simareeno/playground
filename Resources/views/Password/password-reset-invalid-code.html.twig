{% extends 'Theme::layout.html.twig' %}

{% block page_content %}
    <div class="breadcrumbs">
        {% show breadcrumbs %}
    </div>
    <p>{{ phrase('portal.account.reset-password-invalid-desc') }}</p>

    <p><a href="{{ path('portal_reset_password') }}">{{ phrase('portal.account.reset-password-click-here') }}</a></p>
{% endblock page_content %}
